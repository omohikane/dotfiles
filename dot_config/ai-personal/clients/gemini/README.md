# Gemini Client Adapter

This directory contains configuration notes and adapter scripts specific to the Gemini CLI.

It must not contain core profile definitions.

If Gemini-specific workflow adjustments are needed, they should be implemented here without modifying the core framework.
